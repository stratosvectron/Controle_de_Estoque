CREATE TABLE CONTATOS (
	ID_CONTATOS INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NOME VARCHAR(60),
	TELEFONE VARCHAR(15),
	CELULAR VARCHAR(15),
	EMAIL VARCHAR(100),
	SITE VARCHAR(100),
	CONSTRAINT CONTATOS_PK PRIMARY KEY (ID_CONTATOS)
);
